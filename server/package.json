{
  "name": "d1-automation-server",
  "version": "1.0.0",
  "description": "Backend para automatizaciones de sistema en Windows",
  "type": "module",
  "bin": {
    "optimizador": "./cli.js"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
